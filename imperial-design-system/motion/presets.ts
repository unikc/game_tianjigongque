export const motionPresets = {
  feedback: {
    duration: "var(--ids-duration-fast)",
    easing: "var(--ids-ease-enter)",
  },
  interface: {
    duration: "var(--ids-duration-standard)",
    easing: "var(--ids-ease-enter)",
  },
  scene: {
    duration: "var(--ids-duration-scene)",
    easing: "var(--ids-ease-enter)",
  },
} as const;
